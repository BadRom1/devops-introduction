---
phase: 01-nettoyage-technique
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [slides.md]
autonomous: true

must_haves:
  truths:
    - "Le texte des slides ne contient plus de fautes de grammaire sur les mots identifies (apportes, conference, resume, pose)"
    - "Les 3 balises </img> orphelines sont supprimees et les images s'affichent correctement"
    - "Les 2 QR codes de la slide finale pointent vers BadRom1 (pas badrom1)"
    - "La presentation se build sans erreur apres les corrections"
  artifacts:
    - path: "slides.md"
      provides: "Contenu de la presentation corrige"
      contains: "apporter"
  key_links:
    - from: "slides.md QR codes"
      to: "https://BadRom1.github.io/devops-introduction"
      via: "data prop on QRCode components"
      pattern: 'data="https://BadRom1.github.io/devops-introduction"'
---

<objective>
Corriger les coquilles textuelles, les balises HTML mal formees et les URLs incoherentes dans les QR codes de la presentation Slidev.

Purpose: Eliminer la dette technique textuelle et HTML avant de refactoriser le CSS (plan suivant). Les corrections textuelles sont prerequises car le plan 02 va restructurer les slides CALMS et il est plus simple de corriger le texte d'abord.
Output: slides.md avec texte correct, HTML valide et URLs coherentes.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-nettoyage-technique/01-RESEARCH.md
@slides.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Corriger les coquilles textuelles francaises</name>
  <files>slides.md</files>
  <action>
Corriger les fautes de grammaire et d'orthographe suivantes dans slides.md. Pour chaque correction, le contexte exact est fourni :

1. **Line 153** : "Lors de ces conférence" -> "Lors de ces conférences" (pluriel manquant)

2. **Line 229** : "apportés le plus rapidement" -> "apporter le plus rapidement" (infinitif requis apres "Leurs preoccupations :")

3. **Line 289** : "les principes fondateurs du DevOps ont été posé" -> "les principes fondateurs du DevOps ont été posés" (accord du participe passe avec le sujet "principes")

4. **Line 291** : "Ils sont résumé par l'acronyme" -> "Ils sont résumés par l'acronyme" (accord du participe passe avec le sujet "Ils")

5. **Line 1105** : "Il est vidé, pour en faciliter" -- Verifier le sens voulu. Le mot "vide" dans le requirement DEBT-01 fait reference a cette ligne. Le sens semble etre "Il est vide de" (stripped of) ou "On l'a vide de". Relire la phrase complete et corriger en preservant le sens : le contenu en ligne est depouille de toute notion de fiabilite. Si "vide" est deja correct dans le contexte (participe passe de "vider"), ne pas modifier.

Apres ces corrections, faire un grep dans tout le fichier pour verifier qu'il n'y a pas d'autres occurrences des memes erreurs (rechercher : "conférence " sans s en fin de mot, "résumé " sans s quand le sujet est pluriel, "posé " sans s avec sujet pluriel).
  </action>
  <verify>
Grep pour verifier les corrections :
- `grep -n "conférence," slides.md` ne retourne rien (pluriel corrige)
- `grep -n "apportés" slides.md` ne retourne rien (infinitif corrige)
- `grep -n "été posé\." slides.md` ou `grep -n "été posé[^s]" slides.md` ne retourne rien
- `grep -n "sont résumé " slides.md` ne retourne rien
  </verify>
  <done>Les 4-5 coquilles identifiees sont corrigees. Le texte francais est grammaticalement correct sur les mots cibles.</done>
</task>

<task type="auto">
  <name>Task 2: Corriger les balises HTML et les URLs QR codes</name>
  <files>slides.md</files>
  <action>
Deux corrections distinctes dans slides.md :

**Partie A : Balises HTML mal formees (DEBT-04)**

Aux lignes 1025-1027, corriger les 3 balises `</img>` orphelines. Les balises `<img>` sont auto-fermantes en HTML -- `</img>` n'est pas valide.

AVANT (lignes 1025-1027) :
```html
<img src="/assets/images/Jenkins_logo.svg.png"></img>
<img src="/assets/images/gitlab-ci-cd-logo.png"></img>
<img src="/assets/images/GitHub_Invertocat_Logo.svg.png"></img>
```

APRES :
```html
<img src="/assets/images/Jenkins_logo.svg.png" />
<img src="/assets/images/gitlab-ci-cd-logo.png" />
<img src="/assets/images/GitHub_Invertocat_Logo.svg.png" />
```

Utiliser la forme auto-fermante `/>` qui est standard en contexte Vue/JSX.

**Partie B : URLs QR codes incoherentes (DEBT-03)**

Aux lignes 1217 et 1240, remplacer `badrom1` par `BadRom1` dans les URLs des composants QRCode.

AVANT ligne 1217 :
```
data="https://badrom1.github.io/devops-introduction"
```
APRES :
```
data="https://BadRom1.github.io/devops-introduction"
```

Meme correction ligne 1240.

Verifier aussi qu'aucune autre occurrence de `badrom1` n'existe dans le fichier.
  </action>
  <verify>
1. `grep -n "</img>" slides.md` ne retourne rien (toutes les balises orphelines supprimees)
2. `grep -n "badrom1" slides.md` ne retourne rien (toutes les URLs corrigees)
3. `grep -c "BadRom1" slides.md` retourne 4 (frontmatter, texte, et 2 QR codes)
4. `pnpm run build` dans le repertoire du projet se termine sans erreur
  </verify>
  <done>Les 3 balises `</img>` sont remplacees par des `<img ... />` auto-fermantes. Les 2 QR codes pointent vers `BadRom1.github.io`. Le build Slidev passe sans erreur.</done>
</task>

</tasks>

<verification>
1. Grep systematique pour chaque pattern corrige (voir verify de chaque task)
2. `pnpm run build` sans erreur -- confirme que les modifications HTML sont valides et que Slidev parse correctement le fichier
3. Aucune regression : le nombre total de slides n'a pas change (verifier que pas de `---` supprime ou ajoute par erreur)
</verification>

<success_criteria>
- Zero occurrence de `</img>` dans slides.md
- Zero occurrence de `badrom1` dans slides.md
- Les mots "conférences" (l.153), "apporter" (l.229), "posés" (l.289), "résumés" (l.291) sont corrects
- `pnpm run build` se termine avec exit code 0
</success_criteria>

<output>
After completion, create `.planning/phases/01-nettoyage-technique/01-01-SUMMARY.md`
</output>
